import requests
import xml.etree.ElementTree as ET
import os

def run_monitor():
    # 改动点：如果没有密钥，直接优雅退出，而不是报错停止
    bark_key = os.environ.get("MY_BARK_KEY")
    if not bark_key:
        print("警告: 未检测到 MY_BARK_KEY，本次任务跳过。")
        return  # 直接返回，不会导致任务报错

    bark_base_url = f"https://api.day.app/{bark_key}/"
    # ... 原有的代码逻辑不变 ...
    print("成功运行监控逻辑")

if __name__ == "__main__":
    run_monitor()
