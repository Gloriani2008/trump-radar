import requests
import os

def run_monitor():
    # 获取密钥
    bark_key = os.environ.get("MY_BARK_KEY")
    
    # 检查密钥是否配置，如果没有配置，直接提示并优雅退出
    if not bark_key:
        print("错误: 未检测到 MY_BARK_KEY 环境变量，请检查 GitHub Settings 中的 Secret 配置。")
        return

    # 这里替换成你原本的监控逻辑
    # 比如：
    # response = requests.get("你的目标网址")
    # if response.status_code == 200:
    #     requests.get(f"https://api.day.app/{bark_key}/雷达监控到新消息")
    
    print("监控程序运行成功")

if __name__ == "__main__":
    run_monitor()
